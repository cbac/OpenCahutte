{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('_button_id.html.twig',{'action_path':'user_edit','id_value':user.id,'label':'Edit'}) }}
    {{ include('OCUser/_delete_form.html.twig') }}
    {{ include('_button.html.twig', {'action_path':'user_index','label':'Users List'}) }}
    
{% endblock %}
