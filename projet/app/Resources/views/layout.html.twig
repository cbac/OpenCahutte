<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}OpenCahutte création de quiz en ligne{% endblock %}</title>
  {% block stylesheets %}
    {# On charge le CSS de bootstrap depuis le site directement #}
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
  {% endblock %}
</head>
<body>
  <div class="container">
    <div id="header" class="jumbotron">
	
	  <a href="{{path('oc_quizgen_homepage')}}"><img src="{{ asset('img/logo.png') }}" alt="logo"/></a>
      <h1>Créer des quiz simplement </h1>


      <p>
        Créez un quiz, ajoutez vos questions et testez-vous entre amis.
      </p>
      
      {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
	
	<a class="btn btn-primary btn-lg" href="{{ path('fos_user_security_logout') }}">Déconnexion</a> -
	Connecté en tant que <a href="{{ path('fos_user_profile_show') }}">{{ app.user.username }}</a> - 
      {% else %}
	<a class="btn btn-primary btn-lg" href="{{ path('fos_user_security_login') }}">
          Connectez-vous »
        </a><div><br>
        <a href="{{ path('fos_user_registration_register') }}" class="btn btn-success btn-xs">Inscription</a></div>
      {% endif %} 
      
      
    </div>
    <div class="row">
      <div id="menu" class="col-md-3">
        <h3>Quizs</h3>
        <ul class="nav nav-pills nav-stacked">
          <li><a href="{{ path('oc_quizdis_select') }}">Participer à un quiz</a></li>
          <li><a href="{{ path('oc_quizgen_list') }}">Liste des quiz</a></li>
          <li><a href="{{ path('oc_quizgen_add') }}">Ajouter un quiz</a></li>
        </ul>
        <h4>Derniers quiz</h4>
        {{ render(controller("OCQuizgenBundle:Default:menu", {'limit': 3})) }}
      </div>
      <div id="content" class="col-md-9">
        {% block body %}
          {% block fos_user_content %}
	  {% endblock fos_user_content %}

        {% endblock %}
      </div>
    </div>
    <hr>
    <footer>
      <p>The sky's the limit © {{ 'now'|date('Y') }} and beyond.</p>
    </footer>
  </div>
  {% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  {% endblock %}
</body>
</html> 
