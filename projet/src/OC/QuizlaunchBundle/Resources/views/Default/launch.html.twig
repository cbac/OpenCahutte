
{% extends "OCQuizgenBundle::layout.html.twig" %}

{% block title %}
  Accueil - {{ parent() }}
{% endblock %}

{% block ocquizgen_body %}

 {# <h2>Question : {{ texte }} !</h2> #}
  {% if quiz.QCMs[idq] is defined %}
		{% set QCM = quiz.QCMs[idq] %}
		<li>{{ QCM.question }}</li>
		
		<ul>	{{ QCM.rep1 }}</ul>
		<ul>	{{ QCM.rep2 }}</ul>
		<ul>	{{ QCM.rep3 }}</ul>
		<ul>	{{ QCM.rep4 }}
		</ul>
	  {% else %}
		<li>Pas de réponses trouvées.</li>

		{% endif %}
		{#
<?php
  var newYear = new Date(); 
newYear = new Date(newYear.getFullYear() + 1, 1 - 1, 1); 
$('#defaultCountdown').countdown({until: $date}); 
 
$('#removeCountdown').click(function() { 
    var destroy = $(this).text() === 'Remove'; 
    $(this).text(destroy ? 'Re-attach' : 'Remove'); 
    $('#defaultCountdown').countdown(destroy ? 'destroy' : {until: $date}); 
});

?>
 
  #}
<a href="{{ path('oc_quizlaunch_question', {'id': id, 'gamepin' : gamepin, 'idq': idq+1}) }}">Question suivante</a>


{% endblock %}