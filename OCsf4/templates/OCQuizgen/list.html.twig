{# src/templates/OCQuizgen/list.html.twig #}

{% extends "layout.html.twig" %}

{% block title %}
  Quiz list - {{ parent() }}
{% endblock %}

{% block body %}

  <h2>Quiz List</h2>

  <ul>
    {% for quiz in listQuizs %}
      <li>
        <a href="{{ path('oc_quizgen_view', {'id': quiz.id}) }}">
          {{ quiz.nom }}
        </a>
        par {{ listNoms[loop.index0] }},
		
        le {{ quiz.date|date('d/m/Y') }}
      </li>
    {% else %}
      <li>No Quiz yet.</li>
    {% endfor %}
  </ul>

{% endblock %}