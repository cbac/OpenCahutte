{# src/templates/OCQuizgen/mylist.html.twig #}

{% extends "layout.html.twig" %}

{% block title %}
  My Quiz List - {{ parent() }}
{% endblock %}

{% block body %}

  <h2>Quiz list for {{ auteur }}</h2>

  <ul>
    {% for quiz in listQuizs %}
      <li>
        <a href="{{ path('oc_quizgen_view', {'id': quiz.id}) }}">
          {{ quiz.nom }}
        </a>
		
        le {{ quiz.date|date('d/m/Y') }}
      </li>
    {% else %}
      <li>No Quiz yet.</li>
    {% endfor %}
  </ul>

{% endblock %}